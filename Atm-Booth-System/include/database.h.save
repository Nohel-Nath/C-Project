#ifndef DATABASE_H
#define DATABASE_H
#include<vector>
using namespace std;
class database:
{
    public:
        vector<account>buffer;
        database();
        virtual ~database();
        int find_(account a);
        int find_(string user,string pass);
        bool register_(account a);
        void update_(int idx);
        void read_();
        void write_();
    protected:
    private:
};

#endif // DATABASE_H
